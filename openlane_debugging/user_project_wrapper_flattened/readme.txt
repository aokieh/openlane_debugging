This design contains only the digital cells for the design.

The analog instance has been removed.

It should generate a proper PDN with std cells passing timing.

IR-Drop report was kinda weird here, it couldn't find something in SPEF for clkbuf instances? [WARNING STA-1650]
